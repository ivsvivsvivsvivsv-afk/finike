# 🤖 Telegram Bot для Игрового Дня

Полностью модульный Telegram-бот для регистрации на событие "Игровой день" в Марина Джим (Финика). Вся конфигурация и тексты выносятся в отдельные JSON-файлы для удобного редактирования без перезагрузки кода.

## 📁 Структура проекта

```
board-game-bot/
├── bot.py                    # Основной файл бота
├── config.json               # Конфигурация (токен, пути к изображениям, параметры события)
├── texts.json                # Все тексты для бота (легко редактируются)
├── requirements.txt          # Зависимости Python
├── .gitignore               # Файлы для игнорирования
├── README.md                # Этот файл
│
├── images/                  # Папка с изображениями для бота
│   ├── bot_welcome_banner.png
│   ├── bot_catan_visual.png
│   ├── bot_carcassonne_visual.png
│   ├── bot_dnd_visual.png
│   ├── bot_event_atmosphere.png
│   └── bot_confirmation_scroll.png
│
└── data/                    # Папка с данными (автоматически создаётся)
    └── registrations.json   # Все регистрации (автоматически создаётся)
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/board-game-bot.git
cd board-game-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Запуск бота

```bash
python bot.py
```

Бот будет запущен и начнёт слушать команды в Telegram.

## ⚙️ Конфигурация

### config.json

Главный конфиг-файл с параметрами:

```json
{
  "bot": {
    "token": "ВАШЕ_ЗНАЧЕНИЕ_ТОКЕНА",
    "log_level": "INFO"
  },
  "images": {
    "welcome": "images/bot_welcome_banner.png",
    ...
  },
  "event": {
    "name": "Игровой день в Марина Джим",
    "location": "Кафе Марина Джим, Финика, Турция",
    "date": "Суббота",
    "time_start": "12:00",
    "time_end": "20:00",
    "games": [...]
  }
}
```

**Редактируй это, если:**
- Меняются дата/время события
- Меняются параметры игр (количество игроков, длительность)
- Меняются пути к изображениям

### texts.json

Все текстовые сообщения бота находятся в этом файле. Структура:

```json
{
  "welcome": {
    "text": "🏰 *ДОБРО ПОЖАЛОВАТЬ...* "
  },
  "catan": {
    "text": "🏗️ *КАТАН: ЛЕГЕНДА...* "
  },
  ...
}
```

**Редактируй это, если:**
- Нужно изменить описание игр
- Нужно обновить FAQ
- Нужно изменить тексты сообщений
- Нужно исправить опечатки

**Важно:** Используй `{PLACEHOLDER}` для динамических значений:
- `{NAME}` — имя пользователя
- `{GAMES}` — список игр
- `{GAME}` — название игры

## 📝 Редактирование текстов

### Вариант 1: Прямое редактирование на GitHub

1. Открой `texts.json` на GitHub
2. Нажми ✏️ (Edit this file)
3. Сделай изменения
4. Commit changes
5. **Перезагрузи бота или используй команду `/reload` в Telegram**

### Вариант 2: Через Git локально

```bash
# Измени texts.json или config.json в локальном редакторе
git add texts.json config.json
git commit -m "Update bot texts"
git push origin main
# Перезагрузи бота
```

### Вариант 3: Hot-reload из Telegram (для тестирования)

Отправь команду в чат с ботом:
```
/reload
```

Бот перезагрузит конфиг и тексты без перезапуска.

## 🎮 Использование

### Пользователю

1. Найди бота в Telegram (по ссылке вида `https://t.me/your_bot_username`)
2. Нажми `/start`
3. Выбери интересующую информацию или зарегистрируйся
4. Следуй инструкциям

### Администратору

**Просмотр регистраций:**
```bash
cat data/registrations.json
```

**Сброс регистраций:**
```bash
echo "[]" > data/registrations.json
```

## 🔄 Как работает обновление текстов?

### При редактировании texts.json:

1. ✏️ **Редактирование** → изменяешь JSON на GitHub
2. ⬇️ **Загрузка** → `git pull` на сервере или в Amwera
3. 🔄 **Перезагрузка** → команда `/reload` в боте ИЛИ перезапуск бота
4. ✅ **Готово** → пользователи видят новые тексты

### Без перезагрузки кода:

- Изменения в `texts.json` подтягиваются автоматически через `config.reload()`
- Изменения в `config.json` тоже подтягиваются без перезапуска
- Только добавление НОВЫХ команд требует перезапуска бота

## 📊 Регистрация пользователей

Все регистрации сохраняются в `data/registrations.json`:

```json
[
  {
    "name": "Иван",
    "age": 28,
    "gender": "Мужской",
    "telegram": "ivanov_ivan",
    "user_id": 123456789,
    "games": ["Катан", "Каркассон"],
    "timestamp": "2026-01-18T15:30:45.123456"
  },
  ...
]
```

## 🖼️ Изображения

Все изображения для сообщений бота находятся в папке `images/`. Если изображение не найдено, бот отправит сообщение без фото (fallback).

### Как добавить новое изображение:

1. Добавь файл в папку `images/`
2. Обнови пути в `config.json`:
   ```json
   "images": {
     "new_image": "images/my_new_image.png"
   }
   ```
3. Используй в коде: `send_photo_with_fallback(query, "new_image", text)`

## 🔐 Безопасность

⚠️ **Важно:**
- Не коммитьте `.env` файлы с реальными токенами на GitHub (используй `.gitignore`)
- Токен уже находится в `config.json` — в production используй переменные окружения

## 🚀 Развёртывание на Amwera

1. Загрузи проект на GitHub
2. На Amwera создай новый Python-проект
3. Склонируй репозиторий:
   ```bash
   git clone https://github.com/yourusername/board-game-bot.git
   cd board-game-bot
   ```
4. Установи зависимости:
   ```bash
   pip install -r requirements.txt
   ```
5. Запусти:
   ```bash
   python bot.py
   ```

**Обновление текстов на Amwera:**
```bash
git pull origin main
# Бот автоматически перезагрузит тексты
# или используй /reload в Telegram
```

## 📚 Переменные в текстах

Поддерживаемые плейсхолдеры:

| Плейсхолдер | Описание | Пример |
|-------------|---------|--------|
| `{NAME}` | Имя пользователя | "Добро пожаловать, {NAME}!" |
| `{GAMES}` | Список выбранных игр | "Твои игры: {GAMES}" |
| `{GAME}` | Название одной игры | "{GAME} добавлен!" |

Используются в методе `config.get_text("key", NAME="Ivan", GAMES="Катан, Каркассон")`

## 🐛 Отладка

**Логи бота:**
```
INFO:__main__:🤖 Бот запущен! Токен: 8522444294:AAFAdm3...
INFO:__main__:📁 Хранилище регистраций: data/registrations.json
```

**Проверить синтаксис JSON:**
```bash
python -m json.tool texts.json
python -m json.tool config.json
```

## 📞 Поддержка

Если что-то не работает:

1. Проверь синтаксис JSON
2. Проверь пути к изображениям
3. Проверь логи бота
4. Убедись, что токен валиден

## 📝 Лицензия

MIT

---

**Создано для игрового дня в Марина Джим, Финика, Турция** 🎮⚔️🐉
